# Phase 1 (ETL + DB)
pandas==2.2.2
SQLAlchemy==2.0.30

# Phase 2 (ML training)
scikit-learn==1.5.0
matplotlib==3.9.0
thefuzz==0.22.1
python-Levenshtein==0.25.1

# Phase 3 (FastAPI + Auth)
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.9
openpyxl==3.1.2

# Frontend (Phase 4) dependencies are managed separately via
# frontend/package.json — run `npm install` inside frontend/, not pip.
